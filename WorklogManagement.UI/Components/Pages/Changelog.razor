@page "/changelog"
@rendermode InteractiveServer
@inherits BasePage<ChangelogViewModel>

<PageTitle>Verlauf</PageTitle>

<Content IsLoading="@ViewModel.IsLoading.Value" Error="@ViewModel.LoadError.Value?.ToString()">
    Verlauf
</Content>

@code
{
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {

        }
    }
}
