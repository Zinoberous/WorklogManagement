@inherits LayoutComponentBase

<RadzenComponents />

<RadzenLayout>
    <RadzenSidebar>
        <div class="sidebar-top" />
        <NavMenu />
    </RadzenSidebar>
    <RadzenBody Style="padding: 0">
        @Body
    </RadzenBody>
</RadzenLayout>

@code {
    [Inject]
    public INotifier Notifier { get; set; } = null!;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await Notifier.MarkRenderingCompleteAsync();
        }
    }
}
